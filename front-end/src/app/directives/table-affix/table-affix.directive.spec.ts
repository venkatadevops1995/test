import { TableAffixDirective } from './table-affix.directive';

describe('TableAffixDirective', () => {
  it('should create an instance', () => {
    const directive = new TableAffixDirective();
    expect(directive).toBeTruthy();
  });
});
